function booWho(bool) {
    // What is the new fad diet for ghost developers? The Boolean.
    // This is a simple one. In this, have to return true if it is a boolean primitive. If not false. 
    if(bool === true || bool === false ){
     return true;
    } else
    return false;
  }
  
  booWho(null);